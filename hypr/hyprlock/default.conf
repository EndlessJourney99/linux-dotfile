#  _                      _            _    
# | |__  _   _ _ __  _ __| | ___   ___| | __
# | '_ \| | | | '_ \| '__| |/ _ \ / __| |/ /
# | | | | |_| | |_) | |  | | (_) | (__|   < 
# |_| |_|\__, | .__/|_|  |_|\___/ \___|_|\_\
#        |___/|_|                           
# 

# background {
#     monitor =
#     path = $HOME/.config/ml4w/cache/blurred_wallpaper.png   # only png supported for now
# }
# source = /home/justin/.cache/wal/colors-hyprland.conf

# variables
$BACKGOUND_IMAGE = $HOME/wallpaper/Anime-Purple-eyes.png

# BACKGROUND
background {
    monitor = HDMI-A-1
    #path = screenshotk
    path = $BACKGOUND_IMAGE
    #color = $background
    blur_passes = 0
    contrast = 1
    brightness = 0.6
    vibrancy = 2
    vibrancy_darkness = 0
}

background {
    monitor = eDP-1
    #path = screenshotk
    path = $BACKGOUND_IMAGE
    #color = $background
    blur_passes = 1
    contrast = 1
    brightness = 0.6
    vibrancy = 2
    vibrancy_darkness = 0
}

# GENERAL
general {
    no_fade_in = true
    no_fade_out = true
    hide_cursor = false
    grace = 0
    disable_loading_bar = false
    fractional_scaling = 1
}

input-field {
    monitor = HDMI-A-1
    size = 450, 35
    outline_thickness = 0
    dots_size = 0.25 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.55 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rounding = -1
    rounding = 5
    outline_thickness = 0
    outer_color = rgba(104, 88, 65, 1) 
    inner_color = rgba(104, 88, 65, 1.0) 
    font_color = rgba(0, 0, 10, 1) 
    fade_on_empty = false
    placeholder_text = Input your password # Text rendered in the input box when it's empty.
    check_color = rgba(204, 136, 34, 0)
    fail_color = rgba(204, 34, 34, 0) # if authentication failed, changes outer_color and fail message color
    fail_text = $FAIL <b>($ATTEMPTS)</b> # can be set to empty
    fail_transition = 200 # transition time in ms between normal outer_color and fail_color
    fail_timeout = 500
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = false # see below
    position = 0, 13%
    halign = center
    valign = bottom
}

# TIME
label {
  monitor = HDMI-A-1
  text = cmd[update:1000] echo "$(date +"%-I:%M")"
  color = rgba(199,129,190, 1) 
  font_size = 175
  text_align = center
  font_family = Fragile Bombers Attack
  position = 0, 22%
  halign = center
  valign = bottom
}

# DATE
label {
  monitor = HDMI-A-1
  text = cmd[update:1000] echo "$(date +"%A, %B %d")"
  color = rgba(199,129,190, 1)
  font_size = 50
  font_family = Fragile Bombers Attack 
  position = 0, 18%
  halign = center
  valign = bottom
}


# label {
#   monitor = eDP-1
#   text = cmd[update:1000] echo "$(/home/endless_journey/.config/hypr/scripts/battery.sh)"
#   color = rgba(242, 243, 244, 0.75)
#   font_size = 35
#   font_family = JetBrains Mono
#   position = 0, -10
#   halign = right
#   valign = top
# }

# shape {
#   monitor = 
#   zindex = 1
#   size = 700, 3000
#   color = rgba(0, 0, 0, 0.3)
#   halign  = right
#   position = -200, -100
# }

# label {
#   monitor = HDMI-A-1
#   text = cmd[update:1000] echo "$(/home/endless_journey/.config/hypr/scripts/network-status.sh)"
#   color = rgba(242, 243, 244, 0.75)
#   font_size = 24
#   font_family = SF Pro Text
#   position = -50, -10
#   halign = right
#   valign = top
# }
#
# label {
#   monitor = eDP-1
#   text = cmd[update:1000] echo "$(/home/endless_journey/.config/hypr/scripts/network-status.sh)"
#   color = rgba(242, 243, 244, 0.75)
#   font_size = 35
#   font_family = SF Pro Text
#   position = -70, -10
#   halign = right
#   valign = top
# }
